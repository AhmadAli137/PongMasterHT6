/* Copy to secrets.h (gitignored) and fill in. The bench only needs Wi-Fi
 * credentials — it connects purely to keep the DAOKI awake, not to send data. */
#pragma once

#define WIFI_SSID "YOUR_WIFI_SSID"
#define WIFI_PASS "YOUR_WIFI_PASSWORD"
